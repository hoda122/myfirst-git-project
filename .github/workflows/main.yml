on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest  

    steps:
      - name: 'testing github action'
        run: pwd 


      - name: 'before'
        run: ls

      - uses: actions/checkout@v3

      - name: 'after'
        run: ls